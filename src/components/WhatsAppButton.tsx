'use client';

// Desktop WhatsApp is now integrated into the StickyBottomBar desktop floating bar.
// This component is kept as a no-op so imports don't break.
export default function WhatsAppButton() {
  return null;
}
